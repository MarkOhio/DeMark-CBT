{
  "rules": {
    "exams": {
      "$examId": {
        ".read": true,
        ".write": "auth != null && root.child('admins').child(auth.uid).val() === true",

        "questions": {
          ".read": "now >= root.child('exams').child($examId).child('startTime').val()",
          ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
        }
      }
    },

    "submissions": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() === true",

      "$subId": {
        ".write": "(!data.exists() && newData.child('examId').isString() && newData.child('studentName').isString() && newData.child('studentNumber').isString() && newData.child('answers').exists() && newData.child('submittedAt').isNumber() && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= newData.child('total').val()) || (auth != null && root.child('admins').child(auth.uid).val() === true)",

        "score": {
          ".validate": "newData.isNumber() && newData.val() >= 0",
          ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
        }
      }
    },

    "feedback": {
      ".read": true,

      "$feedbackId": {
        ".write": "(!data.exists() && newData.child('name').isString() && newData.child('email').isString() && newData.child('subject').isString() && newData.child('message').isString() && newData.child('created').isNumber()) || (auth != null && root.child('admins').child(auth.uid).val() === true)",

        "replies": {
          ".read": true,
          ".write": "auth != null && root.child('admins').child(auth.uid).val() === true",

          "$replyId": {
            ".validate": "newData.hasChildren(['message','adminName','created']) && newData.child('message').isString() && newData.child('adminName').isString() && newData.child('created').isNumber()"
          }
        }
      }
    },

    "admins": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() === true",
      ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
    }
  }
}
