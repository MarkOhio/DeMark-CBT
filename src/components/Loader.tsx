
/*
  Loader.tsx commented out. Original loader markup and styles are preserved
  in source control if you need to restore it.
*/

// Keep a tiny stub export so imports don't break the build if this file
// is accidentally imported elsewhere.
export default function _LoaderCommentedOut(): null {
  return null;
}